"""Quarantine is registered beside each entity by framework.silver_model.

Keeping this boundary explicit documents that Silver uses typed
`<entity>_quarantine` materialized views instead of one lossy JSON catch-all.
"""
