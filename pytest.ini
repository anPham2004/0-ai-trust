[pytest]
markers =
    live_dq: executes DQ assert_true() checks against a live, already-deployed Databricks workspace (see tests/integration/silver/conftest.py). Skipped automatically unless DATABRICKS_SERVER_HOSTNAME/DATABRICKS_HTTP_PATH/DATABRICKS_TOKEN are set.
