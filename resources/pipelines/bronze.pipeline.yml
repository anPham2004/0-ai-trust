resources:
  pipelines:
    bronze_ingestion:
      name: 0-ai-trust-bronze
      catalog: ${var.catalog}
      schema: bronze
      serverless: true
      continuous: false
      development: false
      channel: CURRENT
      photon: true
      root_path: ${workspace.file_path}/pipelines/bronze
      libraries:
        - file:
            path: ../../pipelines/bronze/pipeline.py
      configuration:
        zero_ai_trust.catalog: ${var.catalog}
        zero_ai_trust.landing_path: ${var.landing_path}
